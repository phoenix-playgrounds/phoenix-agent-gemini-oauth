services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - WS_URL=${WS_URL:-ws://host.docker.internal:55556/agent-cable}
      - AGENT_SECRET=${AGENT_SECRET:-SECRET}
      - MOCKED_GEMINI=${MOCKED_GEMINI:-1}
